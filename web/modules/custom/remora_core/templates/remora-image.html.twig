<picture>
  {% for breakpoint, image in sources %}
    <source srcset="{{ image }}" media="{{ breakpoint }}"/>
  {% endfor %}
  {% if default_image is defined %}
    <img src="{{ default_image }}" alt="{{ media.field_media_image.alt ?? '' }}"/>
  {% endif %}
</picture>
