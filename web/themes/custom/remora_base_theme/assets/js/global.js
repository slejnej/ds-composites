(function($, Drupal) {
  'use strict';

  Drupal.behaviors.remora_base_theme_global = {
    attach: function(context, settings) {
      if (context !== document) {
        return;
      }

    }
  };

})(jQuery, Drupal);
