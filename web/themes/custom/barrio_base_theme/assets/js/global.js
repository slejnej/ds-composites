(function($, Drupal) {
  'use strict';

  Drupal.behaviors.barrio_base_theme_global = {
    attach: function(context, settings) {
      if (context !== document) {
        return;
      }

    }
  };

})(jQuery, Drupal);
